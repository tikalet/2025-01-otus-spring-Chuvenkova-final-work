server:
  port: 8080

spring:
  application:
    name: lis
  datasource:
    url: jdbc:postgresql://localhost:5432/lis
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
    initialization-mode: always
  sql:
    init:
      mode: always
      platform: postgresql

mybatis:
  mapper-locations: classpath:mappers/**/*.xml
  type-aliases-package: ru.otus.laboratory.model

rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest

custom-security:
  publicKey: classpath:app.pub
  privateKey: classpath:app.key
